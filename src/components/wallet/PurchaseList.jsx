import React from 'react';
import {Text, View} from 'react-native';

const PurchaseList = () => {
  return (
    <View>
      <Text style={{color: '#fff'}}>Purchase List</Text>
    </View>
  );
};

export default PurchaseList;
