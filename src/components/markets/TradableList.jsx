import React, {memo} from 'react';
import {Text, View} from 'react-native';

const TradableList = memo(() => {
  return (
    <View>
      <Text style={{color: '#fff'}}>Tradable List</Text>
    </View>
  );
});

export default TradableList;
